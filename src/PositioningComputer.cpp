#include "Robot.h"

int Mobius::positioningComputer() {
    while (true) {
        // Odometry
        // GPS
        // Inertial sensor
        // Check for accuracy

        // Vision?
        vex::task::sleep(20);
    }
    return 0;
}